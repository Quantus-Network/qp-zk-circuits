[package]
authors.workspace = true
description.workspace = true
edition.workspace = true
license.workspace = true
name = "qp-wormhole-verifier"
readme = "README.md"
version.workspace = true

[dependencies]
anyhow = { workspace = true }
qp-plonky2-verifier = { version = "=1.5.5", default-features = false }
qp-wormhole-inputs = { version = "=4.4.0", path = "../inputs", default-features = false }
qp-zk-circuits-proof = { version = "=4.4.0", path = "../../proof", default-features = false, features = ["verifier"] }
tiny-keccak = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[features]
default = ["std"]
std = [
	"anyhow/std",
	"qp-plonky2-verifier/std",
	"qp-wormhole-inputs/std",
	"qp-zk-circuits-proof/std",
]

[lints]
workspace = true

[[bench]]
harness = false
name = "verifier"
